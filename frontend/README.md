# frontend (Next.js)

学習UI（試験風UI + 進捗表示）のフロントエンドです。

起動方法やプロジェクトの方針（将来: バックエンド追加でユーザー別に問題セット/進捗を管理）は、リポジトリ直下の `README.md` を参照してください。

## Supabase（Auth）

`/auth` から Cognito Hosted UI でログインできます。

- 環境変数の設定: `frontend/ENVIRONMENT.md` を参照
